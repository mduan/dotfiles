set visualbell t_vb=  " Turn off the visual bell (gets rid of flicker)
set winaltkeys=no     " Disallow menu access using the Alt key

" Turn off scrollbars and other unnecessary menu items. The initial += is a bug workaround
set guioptions+=LlRrbT
set guioptions-=LlRrbT

set guifont=Monaco:h12
